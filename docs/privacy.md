# Privacidad

Las auditorías y perfiles se almacenan exclusivamente en `.seo-data/`, fuera del control de versiones. Los refresh tokens y clientes OAuth se almacenan en el Llavero de macOS, no en archivos del repositorio. El dashboard enlaza únicamente con `127.0.0.1` y no ofrece rutas de escritura.
